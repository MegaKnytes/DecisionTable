plugins {
    id 'maven-publish'
    id 'com.android.library'
}

dependencies {
    implementation 'org.firstinspires.ftc:RobotServer:10.2.0'
    implementation 'org.firstinspires.ftc:RobotCore:10.2.0'
    implementation 'org.firstinspires.ftc:Hardware:10.2.0'
    implementation 'org.firstinspires.ftc:FtcCommon:10.2.0'
}


android {
    compileSdkVersion 34
    namespace "com.decisiontable.ftc.core"
    defaultConfig {
        minSdkVersion 24
        targetSdkVersion 34
        versionCode 1
        aaptOptions {
            ignoreAssetsPattern '!.svn:!.git:!.ds_store:!*.scc:!CVS:!thumbs.db:!picasa.ini:!_*'
        }
    }
    publishing {
        singleVariant("release") {
            withSourcesJar()
            withJavadocJar()
        }
    }
}

publishing {
    publications {
        release(MavenPublication) {
            groupId = 'com.decisiontable.ftc'
            artifactId = 'core'
            version = System.getenv("GITHUB_REF_NAME") ?: "localDev"

            afterEvaluate {
                from components.release
            }
        }
    }
    repositories {
        maven {
            name = "Core"
            url = "https://maven.pkg.github.com/MegaKnytes/DecisionTable"
            credentials {
                username = System.getenv("GITHUB_ACTOR")
                password = System.getenv("GITHUB_TOKEN")
            }
        }
    }
}